package base;

// TestNG-based BaseTest was moved to src/test/java/base/BaseTest.java to avoid compile-time
// dependency on test-scoped libraries (TestNG). This stub keeps the package/class available
// for any non-test compile-time references.
public class BaseTest {
    // Intentionally empty in main sources. Use the TestNG version in test sources for test runs.
}